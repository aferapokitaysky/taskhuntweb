# TaskHunt — план доведения до прода

Аудит проведён по факту (грепом по коду, не по памяти). Изначально —
2026-07-28, обновлён 2026-07-29 после закрытия P0 и монетизации.
Дальше работают только Claude (оркестратор) и Gemini — см.
[`orchestration/README.md`](../orchestration/README.md).

## Текущий снапшот (2026-07-29)

**P0 закрыт полностью.** Prisma-миграции, реальный payout-воркер (с
откатом при неудаче), rate limiting, health-check, CORS, structured
logging (pino), email-верификация, password reset, CI. **Монетизация
закрыта полностью**: подписки Starter/Pro/Premium с реальной
комиссией/лимитами по тиру, продвижение заказов/профилей, комиссия за
вывод. **P1 фронт-хвосты закрыты**: вывод средств, профиль, поддержка,
тарифы, буст заказа.

Что готово и покрыто тестами: auth (email+OAuth+верификация+reset),
wallet/ledger (double-entry, эскроу, payout, комиссии), orders/bids/
milestones/delivery/reviews (+лимиты по тиру, сортировка по буст-
статусу), чат с инвойсами, support-тикеты (бэк+фронт), admin RBAC +
разрешение споров, файлы (S3 + реальный ClamAV), реферальная программа,
подписки/продвижение, notifications-service (реальный email) и
fraud-service — оба воркера рабочие. 76 юнит-тестов, все зелёные.

Дальше — новые пробелы, найденные при аудите 07-29 (их не было в
исходном списке), плюс то, что осталось из P1/P2.

---

## P0 — блокеры реального запуска (ЗАКРЫТО)

Без этого **нельзя** пускать пользователей с реальными деньгами.

### 1. Нет Prisma-миграций
`apps/api/prisma/migrations/` физически не существует — ни у одного из
трёх сервисов (api, notifications-service, fraud-service). Схема
накатывалась через `prisma generate`/неявный push, истории изменений
нет. Следствия: `prisma migrate deploy` (единственно правильный способ
прогнать схему в проде) не сработает — деплоить нечего; откат
невозможен; при первом же расхождении схемы между окружениями поймаем
даунтайм.

**Нужно:** `prisma migrate dev --name init` в каждом из трёх сервисов,
закоммитить `prisma/migrations/`, дальше — только через миграции, без
`db push` в prod-путях.

### 2. Вывод денег не работает по-настоящему
`WalletService.requestWithdrawal()` списывает `WITHDRAWABLE` и пишет
ledger-транзакцию `WITHDRAWAL` — и всё. Комментарий в коде честно
признаёт: "фактическая крипто-выплата — асинхронный процесс вне ledger
(воркер, который дёргает NOWPayments payout API)" — но этого воркера
физически нет. Пользователь запрашивает вывод, баланс списывается,
крипта никуда не уходит.

**Нужно:** воркер (BullMQ job на очереди `withdrawal-payouts`), который
по `WITHDRAWAL`-транзакции реально дёргает NOWPayments payout API,
и по неуспеху — откатывает деньги обратно через `REFUND`-транзакцию
(деньги никогда не должны "зависать" неопределённо).

### 3. Нет верификации email
`User.status` стартует как `PENDING_VERIFICATION`, но `submitOnboarding()`
переводит в `ACTIVE` просто по факту заполнения анкеты — без единой
проверки, что email вообще принадлежит регистрирующемуся. Любой может
зарегистрироваться на чужой адрес.

**Нужно:** таблица/поле для verification-токена, письмо со ссылкой
(через уже готовый notifications-service), эндпоинт подтверждения,
страница на фронте.

### 4. Нет восстановления пароля
Забыл пароль — тупик. Стандартный forgot/reset-password флоу отсутствует
полностью.

### 5. Нет rate limiting
`/auth/login`, `/auth/register`, `/wallet/invoices` и остальные открыты
без единого ограничения запросов — открытая дверь для brute-force и
спама.

### 6. Нет structured logging / error tracking
Везде голый `console.log`/Nest `Logger` (тоже в stdout). В проде это
означает: логи теряются при рестарте контейнера, нет корреляции
запрос→ошибка (request id), нет алертинга на 500-е. Нужен pino (или
аналог) + Sentry (или аналог) хотя бы на `apps/api`.

### 7. CORS открыт с дырой в WebSocket
`chat.gateway.ts`: `cors: { origin: '*' }` — любой сайт может открыть
WebSocket-соединение к чату от имени браузера залогиненного
пользователя. HTTP CORS в `main.ts` тоже: `origin: process.env.WEB_PUBLIC_URL ?? '*'`
— с дефолтом `*`, если забыть проставить env в проде.

### 8. Нет health-check эндпоинта
Ни `GET /health`, ни `/ready` — Docker healthcheck/будущий k8s liveness
не может определить, жив ли `api` (сравни с тем, что у `postgres`/`redis`/
`clamav` в `docker-compose.yml` healthcheck уже есть, а у `api` — нет).

### 9. Нет CI
Папки `.github/` нет вообще. 47 написанных тестов никто не гоняет
автоматически — можно сломать что угодно и не заметить до продакшена.

---

## P1 — важно для нормального UX

10-18 закрыты (вывод средств/профиль/поддержка на фронте, E2E — см.
раунд 6 ниже, graceful shutdown — см. раунд 6). Осталось:

- **In-app уведомления** — событие есть, письмо уходит, но внутри
  приложения (колокольчик/список) уведомления не видно
- **Admin: управление категориями/навыками** — только через
  `prisma/seed.ts`
- **Admin: дашборд с метриками** — выручка, активные споры, воронка —
  ничего этого нет
- **Поиск и фильтры по заказам** — `GET /orders` фильтрует только по
  `categoryId`/`status`
- **E2E-тест критического пути** — юнит-тесты изолированы моками,
  нет теста "всё вместе работает" на реальной БД
- **Graceful shutdown в `apps/api`** — есть у воркеров, нет у самого API

## Новые пробелы (найдены при аудите 07-29, не было в исходном списке)

24. **Нет публичного профиля фрилансера/клиента.** Заказчик видит отклик
    (`bid.freelancer.profile.displayName`), но кликнуть и посмотреть
    полный профиль (портфолио, навыки, отзывы, статистику) негде — нет
    ни `GET /users/:id` (публичного), ни страницы `/freelancers/[id]`
    на фронте. Для маркетплейса, где решение "нанять или нет" критично
    завязано на доверии, это существенная дыра.
25. **Нет страницы поиска/каталога фрилансеров.** Можно просмотреть
    заказы (`GET /orders`), но нельзя посмотреть список фрилансеров по
    категории/навыку — заказчик не может сам никого найти, только ждать
    откликов.
26. **Legal-страницы отсутствуют.** Terms of Service, Privacy Policy,
    cookie-consent — при регистрации/оплате криптой это не опция, а
    необходимость.

---

## P2 — усиление и полировка

27. Тестовое покрытие: `chat.service.ts`, `files.service.ts`,
    `catalog.service.ts`, интеграционный тест
    `referrals-events.listener.ts` (`auth.service.ts` уже покрыт).
28. Load-тест денежных путей на реальной Postgres (не моки) — вне
    формата "написать код", ручной прогон, не в этом раунде.
29. Bull Board — визуальный дашборд очередей BullMQ.
30. SEO для публичных страниц заказов/профилей.

---

## Разбивка на раунд 6 (большой)

См. [`orchestration/TZ_GEMINI_6.md`](../orchestration/TZ_GEMINI_6.md) —
Gemini берёт весь бэкенд: публичный профиль, поиск заказов, admin
категории/навыки/метрики, in-app уведомления (модель+listener+эндпоинты),
graceful shutdown, E2E-тесты, доп. юнит-тесты, Bull Board.

Я беру: страницу публичного профиля, каталог фрилансеров, поиск на
фронте, колокольчик уведомлений, admin UI для категорий/метрик, legal-
страницы, SEO. Часть моих пунктов зависит от эндпоинтов Gemini — начинаю
с независимых (legal, SEO), остальное — по мере готовности бэка.
