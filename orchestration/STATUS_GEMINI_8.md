# STATUS — Gemini, раунд 8

## TODO

- [x] `GET /wallet/withdrawal-fee-info`
- [x] `profileCompleteness` + `missingSteps` в `GET /users/me`
- [x] `avgRating`/`reviewsCount` на фрилансерах в `GET /orders/:id`
- [x] Юнит-тесты на все три
- [x] `pnpm --filter @taskhunt/api test` — зелёное (103/103)
- [x] `pnpm --filter @taskhunt/api build` — чисто

## В процессе

- Все задачи Раунда 8 полностью выполнены и проверены. Детали в [`gemini_conv_8.md`](./gemini_conv_8.md).

## Готово

- [x] **Информация о комиссии вывода**: `GET /wallet/withdrawal-fee-info` возвращает процент и фиксированную ставку комиссии за вывод из модели `CommissionRule` (тип `WITHDRAWAL_FEE`).
- [x] **Геймификация профиля**: `GET /users/me` отдаёт точный `profileCompleteness` (0..100) и отсортированный по важности список невыполненных шагов `missingSteps`.
- [x] **Рейтинги откликов**: `GET /orders/:id` обогащает каждого фрилансера в откликах полями `avgRating` и `reviewsCount` через оптимизированный агрегационный запрос без N+1.
- [x] **Тесты и сборка**: Написаны юнит-тесты на все три метода. Все 103/103 тестов прошëли успешно, сборка бэкенда компилируется чисто.

## Вопросы к оркестратору / нужны правки в чужих файлах

1. Вопросов нет, бэкенд Раунда 8 полностью готов.

## Ответы оркестратора

_(оркестратор отвечает здесь)_
